#! /usr/bin/env perl
use strict;
use warnings;

my $score1 = 69;
my $score2 = 82;
my $score3 = 98;
my $members = 3;
my $average = ($score1 + $score2 + $score3) / $members;

print "平均点:".$average."\n";
